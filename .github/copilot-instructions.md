# UX Pattern Oracle — Copilot Instructions

This repository is a self-contained remote MCP server that builds and serves a
knowledge graph of UX and Behavioural Design Patterns.

## Project overview

- **`ingest.py`** — fetches clean markdown via `https://r.jina.ai/{url}` for
  every URL in `links/links.csv` and writes to `raw/`
- **`server.py`** — FastMCP server; compiles `graphify-out/graph.json` on
  startup if missing, then serves 4 MCP tools over HTTP on `$PORT` (default 8000)
- **`wiki/`** — curated `.md` entries that feed the knowledge graph; edit these
- **`raw/`** — auto-generated scraped markdown; do not edit manually
- **`graphify-out/`** — auto-generated by Graphifyy on server startup; do not edit manually

## Links management

The source of truth for URLs is **`links/links.csv`** with columns:
`id, title, url, tags, description`

### Raindrop.io collection workflow

When the user drops a new CSV file into **`links/collections/`**, you must:

1. Read the CSV — it may be in Raindrop.io export format with columns:
   `id, title, note, excerpt, url, folder, tags, created, cover, highlights, favorite`
2. Map columns: `note` or `excerpt` → `description`; all others map by matching name
3. Extract every row that has a non-empty `url`
4. Deduplicate: skip any URL already present in `links/links.csv`
5. Append the new rows to `links/links.csv`
6. Report how many new links were added and which file they came from

**The `ingest.py` script does this automatically** — `merge_collections()` runs
as the first step every time. You can also call it directly:

```python
from ingest import merge_collections
merge_collections()
```

Never delete entries from `links/links.csv` without explicit user confirmation.

## Workflow — adding new content

```
1. Export a Raindrop collection  →  drop CSV in links/collections/<name>.csv
2. uv run --no-project python ingest.py
   ├── merges all CSVs in collections/ into links/links.csv  (deduped by URL)
   ├── fetches markdown for new URLs via r.jina.ai  →  raw/<slug>.md
3. Curate: promote and edit files from raw/ into wiki/
           add [[wiki-links]] to connect related patterns
4. uv run --no-project python server.py
   └── compiles graphify-out/graph.json on first run, skips on subsequent runs
       serves MCP over HTTP on 0.0.0.0:$PORT
```

## MCP tools (server.py)

| Tool | Inputs | Purpose |
|---|---|---|
| `ask_ux_oracle` | `query: str` | Semantic search across the pattern graph |
| `get_pattern_psychology` | `pattern_name: str` | Cognitive / psychological links for a pattern |
| `generate_design_spec` | `pattern_name, target_platform` | Platform-specific design spec scaffold |
| `predict_component_states` | `component_name: str` | Predict UI states from graph neighbours |

## Coding conventions

- Python ≥ 3.12, managed with `uv` — install with `uv sync --no-install-project`
- All `print()` calls use `flush=True` for cloud log streaming
- All paths are `pathlib.Path` objects, never raw strings
- Slugs: lowercase, hyphens only, via `slugify()` in `ingest.py`
- `ingest.py` uses `requests` only — never `httpx` or `markdownify`
- `server.py` reads from `wiki/` and `graphify-out/graph.json` only, never from `links/`
- Graph JSON uses NetworkX node-link format: top-level keys are `"nodes"` and `"links"`

## README maintenance

**After every change to this repository, update `README.md` automatically.**

The README must always reflect the current state of the project. Apply updates
silently as part of completing any task — do not wait to be asked.

### What to keep in sync

| README section | Update when… |
|---|---|
| **How it works** (flow diagram) | data flow, file paths, or step order changes |
| **Project structure** (tree) | files or folders are added, moved, or removed |
| **Setup** | Python version, `uv` commands, or install steps change |
| **Usage** (any sub-step) | workflow, CLI commands, or file locations change |
| **MCP tools** table | a tool is added, removed, or its signature changes |
| **Dependencies** table | `pyproject.toml` dependencies change |

### Rules

- Edit only the sections that are actually stale — do not rewrite unaffected content
- Keep the README tone: concise, technical, imperative
- Do not add a "Changelog" or "Last updated" note
- The file structure tree in the README must always match the real directory layout

## File structure

```
experience-patterns-oracles/
├── .github/
│   ├── copilot-instructions.md   ← you are here; update freely
│   └── workflows/auto-wiki-build.yml
├── links/
│   ├── links.csv                 ← master URL list (id,title,url,tags,description)
│   └── collections/              ← drop Raindrop CSV exports here
├── raw/                          ← auto-generated scraped markdown (do not edit)
├── wiki/                         ← curated wiki entries (edit and link these)
├── graphify-out/                 ← auto-generated knowledge graph (do not edit)
│   └── graph.json
├── ingest.py                     ← merge collections + fetch markdown
├── server.py                     ← FastMCP server + 4 tools
└── pyproject.toml                ← dependencies (uv)
```
