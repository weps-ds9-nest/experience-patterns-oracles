name: Auto Ingest

on:
  push:
    branches: [main]
    paths:
      - 'links/links.csv'
      - 'links/collections/**'
      - 'ingest.py'

jobs:
  ingest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync --no-install-project

      - name: Run ingest
        run: uv run python ingest.py

      - name: Commit fetched markdown
        uses: stefanzweifel/git-auto-commit-action@8750aaed8f7a1f3e350f8f0ab66a6fea4f3b442b
        with:
          commit_message: 'chore: auto-ingest new links [skip ci]'
          file_pattern: 'raw/**'
